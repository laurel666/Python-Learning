# Loops

Тема будет подробно заполнена после прохождения `variables` и `conditions`.

План для этой темы:

- `while` и `for`
- `range`
- `break`, `continue`, `else`
- `enumerate`
- аккумуляторы и счётчики
- типичные ошибки в циклах
- важные нюансы и Pythonic-подход
