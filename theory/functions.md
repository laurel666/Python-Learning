# Functions

Тема будет подробно заполнена после прохождения базовых тем.

План для этой темы:

- объявление функций
- параметры и возвращаемые значения
- область видимости
- `*args`, `**kwargs`
- чистые функции
- mutable default arguments
- функции как объекты
