# Feedback

Здесь будет накапливаться обратная связь по решённым задачам.

## Тема: variables

Variables_01 — ✔ отлично

Замечание:
решение корректное. Хорошо, что ты последовательно очистил строки, собрал `full_name`,
посчитал возраст и вывел результат через `f-string`.

Что особенно хорошо:

- использован правильный набор методов: `strip()` и `title()`;
- имена переменных понятные;
- вычисления и вывод читаются легко;
- результат совпадает с ожидаемым.

Pythonic вариант:

```python
first_name = " анна ".strip().title()
last_name = "иванова  ".strip().title()
city = "  москва".strip().title()
birth_year = 2001
current_year = 2026

full_name = f"{first_name} {last_name}"
age = current_year - birth_year

print(f"{full_name}, {age} лет, г. {city}")
```

Почему твой вариант тоже хороший:
ты сначала сохранил исходные значения, а потом отдельно их обработал.
Для обучения это даже полезнее, потому что шаги видны явно.

Variables_02 — ✔

Замечание:
решение корректное, формулы посчитаны верно, а вывод оформлен аккуратно через `.2f`.

Что особенно хорошо:

- правильно посчитана стоимость товаров без скидки;
- верно вычислен размер скидки в деньгах;
- итоговая сумма считается в правильном порядке;
- денежные значения оформлены единообразно и читаемо.

Что можно улучшить:

- имя `total_price` здесь работает, но чуть точнее было бы назвать переменную `subtotal`,
  потому что это сумма товаров до скидки и доставки;
- можно отделять вычисления от вывода пустой строкой, чтобы код читался ещё легче.

Pythonic вариант:

```python
item_price = 1899.90
quantity = 2
discount_percent = 15
delivery_price = 249

subtotal = item_price * quantity
discount_amount = subtotal * discount_percent / 100
total = subtotal - discount_amount + delivery_price

print(f"Товары: {subtotal:.2f} руб.")
print(f"Скидка: {discount_amount:.2f} руб.")
print(f"Доставка: {delivery_price:.2f} руб.")
print(f"Итого: {total:.2f} руб.")
```

Почему твой вариант хороший:
ты уже уверенно используешь промежуточные переменные вместо того, чтобы писать всё в одной длинной формуле.
Это делает код понятнее и проще для отладки.

Variables_03 — ✔

Замечание:
решение корректное. Общее количество секунд посчитано верно, строка отчёта совпадает с ожидаемой,
а дополнительная переменная `total_minutes` рассчитана правильно.

Что особенно хорошо:

- формула для перевода времени в секунды составлена верно;
- есть отдельная переменная для полного количества минут;
- вывод собран аккуратно через `f-string`.

Что можно улучшить:

- сейчас `total_minutes` считается, но не используется. Это не ошибка, потому что по условию её нужно посчитать,
  но в дальнейшем полезно следить за тем, чтобы лишние неиспользуемые переменные не оставались в коде без причины.

Pythonic вариант:

```python
hours = 1
minutes = 47
seconds = 35

total_seconds = hours * 3600 + minutes * 60 + seconds
total_minutes = hours * 60 + minutes

print(f"Звонок длился {hours} ч {minutes} мин {seconds} сек ({total_seconds} секунд)")
```

Почему твой вариант хороший:
он уже практически совпадает с аккуратным рабочим решением.

Variables_04 — ✔ отлично

Замечание:
решение корректное и очень аккуратное. Все вычисления верны, а строка вывода полностью совпадает с ожидаемой.

Что особенно хорошо:

- логика расчёта свободного места понятна;
- проценты вычислены по правильной формуле;
- имена переменных читаемые и хорошо отражают смысл;
- форматирование через `.2f` использовано уверенно и к месту.

Pythonic вариант:

```python
disk_total_gb = 512
disk_used_gb = 378.4

disk_free_gb = disk_total_gb - disk_used_gb
percent_used = disk_used_gb / disk_total_gb * 100
percent_free = disk_free_gb / disk_total_gb * 100

print(
    f"Использовано: {disk_used_gb:.2f} GB ({percent_used:.2f}%), "
    f"свободно: {disk_free_gb:.2f} GB ({percent_free:.2f}%)"
)
```

Почему твой вариант хороший:
по сути это уже готовое рабочее решение без лишних действий и без стилистического мусора.

Variables_05 — ✔

Замечание:
решение корректное. Ты правильно преобразовал строку в число, верно посчитал сумму в рублях,
комиссию и итог зачисления.

Что особенно хорошо:

- не забыто преобразование через `float()`;
- формула комиссии составлена верно;
- итоговая сумма считается в правильном порядке;
- денежные значения выведены через `.2f`.

Что можно улучшить:

- в тексте вывода есть маленькая опечатка: `пополнеия` лучше исправить на `пополнения`;
- строку вывода можно разбить на 2-3 `print()`, если хочется сделать чек визуально удобнее.

Pythonic вариант:

```python
usd_amount = float("125.5")
usd_to_rub_rate = 91.37
fee_percent = 1.8

rub_amount = usd_amount * usd_to_rub_rate
fee = rub_amount * fee_percent / 100
final_amount_rub = rub_amount - fee

print(f"Сумма в рублях: {rub_amount:.2f}")
print(f"Комиссия: {fee:.2f}")
print(f"К зачислению: {final_amount_rub:.2f}")
```

Почему твой вариант хороший:
ты не попытался написать всё в одну сложную формулу и выделил промежуточные шаги в отдельные переменные.

Variables_06 — ✔ отлично

Замечание:
решение полностью корректное. Ты правильно распаковал кортеж, обработал имя пользователя
и собрал итоговую строку в точном формате.

Что особенно хорошо:

- распаковка кортежа использована именно так, как и нужно в этой задаче;
- переменная `raw_user_name` названа очень удачно;
- очистка и форматирование имени сделаны отдельно и читаемо;
- итоговая строка `summary` собрана аккуратно через `f-string`.

Pythonic вариант:

```python
user_data = (104, "  elena petrova ", "premium", True)

user_id, raw_user_name, plan, is_active = user_data
user_name = raw_user_name.strip().title()
summary = f"ID: {user_id} | User: {user_name} | Plan: {plan} | Active: {is_active}"

print(summary)
```

Почему твой вариант особенно хороший:
ты не просто решил задачу, а сделал это понятным и профессионально читаемым способом.

Variables_07 — ✔

Замечание:
решение корректное. Имя гостя очищено правильно, итог посчитан верно, а чек собран в многострочную строку.

Что особенно хорошо:

- ты использовал многострочную строку именно по задаче;
- имя очищено и приведено к аккуратному виду;
- итоговая сумма выделена в отдельную переменную;
- вывод получился очень читаемым.

Что можно улучшить:

- переменную `guest_name` можно было сначала оставить как "сырую", а затем отдельно создать `clean_guest_name`,
  но это именно стилистическое улучшение, а не ошибка.

Pythonic вариант:

```python
drink_name = "Cappuccino"
drink_price = 240
dessert_name = "Cheesecake"
dessert_price = 320
guest_name = "  maria ".strip().title()

total_sum = drink_price + dessert_price
receipt = f"""Guest: {guest_name}
{drink_name}: {drink_price}
{dessert_name}: {dessert_price}
Total: {total_sum}"""

print(receipt)
```

Variables_08 — ✔

Замечание:
решение корректное. Объём посчитан верно, стоимость доставки по весу тоже вычислена правильно.

Что особенно хорошо:

- формула объёма составлена верно;
- стоимость доставки рассчитана без лишних действий;
- итоговая строка содержит все нужные части отчёта.

Что можно улучшить:

- `weight_cost` можно назвать чуть точнее: `delivery_cost`;
- так как это денежное значение, вывод можно оформить через `.2f`, чтобы получить `552.50` вместо `552.5`.

Pythonic вариант:

```python
length_cm = 40
width_cm = 30
height_cm = 20
weight_kg = 6.5
price_per_kg = 85

volume_cm3 = length_cm * width_cm * height_cm
delivery_cost = weight_kg * price_per_kg
report = (
    f"Dimensions: {length_cm} cm x {width_cm} cm x {height_cm} cm, "
    f"Volume: {volume_cm3} cm3, Delivery Cost: {delivery_cost:.2f}"
)

print(report)
```

Variables_09 — ✔ отлично

Замечание:
решение полностью корректное. Ты сделал именно независимую копию списка и наглядно показал,
что после изменения `cart_items` резервная копия не изменилась.

Что особенно хорошо:

- использован правильный метод `copy()`;
- задача на различие между ссылкой и копией решена точно по смыслу;
- вывод хорошо демонстрирует результат.

Pythonic вариант:

```python
cart_items = ["milk", "bread", "tea"]
cart_backup = cart_items.copy()

cart_items.append("coffee")

print("Cart items:", cart_items)
print("Cart backup:", cart_backup)
```

Variables_10 — в работе

Замечание:
решение корректное. Ты правильно очистил строки, преобразовал типы и отдельно показал,
что `age` стал `int`, а `height` стал `float`.

Что особенно хорошо:

- сохранено разделение между сырыми значениями и обработанными;
- имя, фамилия и город приведены к аккуратному виду;
- типы преобразованы именно так, как требовалось по условию;
- итоговая строка профиля собрана понятно и читаемо.

Что можно улучшить:

- вывод типов через `type(...)` корректный, но в будущем можно будет отдельно учиться
  печатать более пользовательский вывод, если задача не учебная, а прикладная.

Pythonic вариант:

```python
raw_first_name = "   ivan"
raw_last_name = "petrov  "
raw_age = "29"
raw_height = "181.4"
raw_city = "  saint petersburg "

first_name = raw_first_name.strip().title()
last_name = raw_last_name.strip().title()
city = raw_city.strip().title()
age = int(raw_age)
height = float(raw_height)

profile_summary = f"{first_name} {last_name}, {age} years, {height} cm, {city}"

print(profile_summary)
print(f"Type of age: {type(age)}")
print(f"Type of height: {type(height)}")
```

Почему твой вариант хороший:
это уже уверенное решение, в котором видно понимание подготовки данных, преобразования типов и форматирования вывода.

## Тема: conditions

Conditions_01 — ✔

Замечание:
решение корректное. Условие вынесено в отдельную булеву переменную, что делает код читаемым.

Conditions_02 — ✔

Замечание:
решение корректное. Тернарный оператор использован уместно и по смыслу делает именно то, что нужно.

Conditions_03 — ✔ с маленькой неточностью

Замечание:
логика доступа посчитана правильно, но в тексте вывода есть маленькое расхождение с условием:
вместо `разрешён` напечатано `разрешен`.

Conditions_04 — ✔

Замечание:
решение корректное. Порядок `if` / `elif` выбран правильно — от более крупных порогов к меньшим.

Conditions_05 — ✔

Замечание:
решение корректное. Особенно хорошо, что роль сначала нормализуется, а потом используется в проверке доступа.

Что можно улучшить:

- запись `role = role and role.strip().lower()` рабочая, но для текущего уровня чуть прозрачнее выглядело бы
  обычное `role = role.strip().lower()`, потому что в задаче `role` гарантированно строка.

Conditions_06 — ✔

Замечание:
логика расчёта комиссии корректная. Для суммы `18500` получается правильный результат `185.0`.

Что можно улучшить:

- если хочешь более "денежный" вывод, можно печатать комиссию через `.2f`.

Conditions_07 — в работе
Conditions_08 — в работе
Conditions_09 — в работе
Conditions_10 — в работе
